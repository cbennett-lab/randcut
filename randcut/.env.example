# Copy to .env and fill in. .env is gitignored — never commit real keys.
# The same variables go into Railway under Project → Variables.

# Google Drive API key (Drive API enabled, no OAuth needed for public folders).
# https://console.cloud.google.com → APIs & Services → Credentials → Create API Key
GOOGLE_API_KEY=
